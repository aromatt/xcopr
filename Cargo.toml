[workspace]
members = ["xcopr-bin", "xcopr"]
resolver = "2"

[workspace.dependencies]
clap = { version = "4", features = ["derive", "wrap_help"] }
pretty_assertions = "1"
# shell-escape = "0.1"  # Pinned for potential future use
# tempfile = "3"  # May be useful for testing or temp file handling